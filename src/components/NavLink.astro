---
const { href, label, class: className } = Astro.props
const currentPath = new URL(Astro.request.url).pathname

const linkClasses = [
  'flex items-center gap-4  text-neutral-500 hover:text-primary/90 [&.is-active]:text-primary [&.is-active]:font-medium py-1 transition duration-300 ease-in-out',
  className,
  href === currentPath ? 'is-active' : '',
]
---

<li>
  <a class:list={[linkClasses]} href={href}>
    <span>{label}</span>
  </a>
</li>
